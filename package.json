{
  "name": "snoozybot",
  "version": "1.0.0",
  "description": "The Twitter bot version of Snoozy.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel -w src/ -d dist/",
    "start": "node dist/app.js",
    "buildrun": "npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0"
  },
  "dependencies": {
    "dotenv": "^2.0.0"
  }
}
